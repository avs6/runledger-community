"""Hands-on lab workbook — NOT auto-run scenarios.

The modules under ``labs/agents/`` are manual, standalone scripts (run by hand,
with optional heavy deps like openai / opentelemetry). ``scenarios.discover()``
deliberately skips this whole subpackage — see ``_SKIP_PACKAGES`` in
``scenarios/__init__.py``. See ``labs/README.md`` for the workbook.
"""
