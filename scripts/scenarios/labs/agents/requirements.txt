# RunLedger lab agents — Python dependencies.
#
# 1) Install the RunLedger SDK from this repo (editable), with the OpenAI extra.
#    Run this from THIS folder (scripts/scenarios/labs/agents):
#
#        pip install -e "../../../../packages/sdk[openai]"
#
#    (Not on PyPI yet — that's why it's a local editable install, not a line below.)
#
# 2) Then install the rest:
#
#        pip install -r requirements.txt

# LLM client (used against local Ollama's OpenAI-compatible endpoint)
openai>=1.0.0

# Config + HTTP
python-dotenv>=1.0.0
httpx>=0.27.0

# OpenTelemetry (Lab 02 — out-of-band OTLP export)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
