# Observe — GAP Matrix

Last updated: 2026-08-15

## Legend

| Status | Meaning |
|--------|---------|
| `PENDING` | Not yet audited |
| `OK` | Verified working |
| `PARTIAL` | Present but partial, buggy, or unclear |
| `MISSING` | Missing, broken, or disconnected |
| `LEGACY` | Legacy/transitional surface; do not expand |

---

## Feature Rows

| Feature | Route | Backend | UI | Actions | Docs | Postman | Scripts/Examples | Complete | Cohesion | Merge / Collapse | Fix Order | Fix Status | Delivery Audit Crosswalk | Notes |
|---------|-------|---------|----|---------|------|---------|------------------|----------|----------|------------------|-----------|------------|--------------------|-------|
| Workspace dashboard | `/dashboard` | `OK` | `OK` | `OK` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `OK` | `OK` | Merged into `Analytics overview` as a compatibility redirect; do not keep a separate first-class dashboard shell. | P3 | `RE-AUDITED: OK` | `3.1` | Re-audited Saturday, August 15, 2026. `/dashboard` is intentionally no longer a first-class Observe feature: the route is a direct UI redirect into `/analytics?scope=workspace&view=overview`, the Analytics overview page is the real owner, and the docs now describe the collapse explicitly. Remaining weakness is support-surface wording that still says “dashboard” broadly in README/demo assets rather than always naming `/analytics` as the canonical owner. |
| Analytics overview | `/analytics` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `OK` | `OK` | `PARTIAL` | Primary scoped overview shell for workspace, org, and platform; keep detailed workspace charts under `Analytics Breakdown` instead of parallel dashboard homes. | P3 | `RE-AUDIT REQUIRED` | `3.8` | Re-audited Saturday, August 15, 2026. `/analytics` is the real Observe entry shell: scope-aware overview cards, top intents/models, recent activity, and direct pivots into Runs, Request Flow, Request Explorer, and economics surfaces are live in code, backed by `/analytics/scoped-summary` plus org rollup support. The feature is strong as an investigative overview, but its broader cohesion remains partial because many cross-feature Observe matrix cells are still unaudited or only lightly bridged in support surfaces. External-rubric N/A review note: an observability overview in an AI workflow platform should usually connect to spend, runtime policy, identity scope, and governance posture, so multiple current `N/A` cohesion cells need re-audit. |
| Runs list | `/runs` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `OK` | `PARTIAL` | Keep separate. | P3 | `RE-AUDIT REQUIRED` | `3.2` | Re-audited Saturday, August 15, 2026. The run list is a real investigative surface: backend filtering, cursor pagination, CSV export, and UI drill-in are all live, and the page explicitly bridges into Request Flow and Request Explorer. The feature itself is strong, but the family remains only partially closed because support/examples are more adjacent than dedicated and the broader Observe cohesion matrix is still largely unaudited. External-rubric N/A review note: run ledgers are normally central to spend explanation, identity scope, runtime evidence, and policy investigation, so several current `N/A` cells should be revisited. |
| Run detail | `/runs/{run_id}` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `OK` | `PARTIAL` | Keep separate. | P3 | `RE-AUDIT REQUIRED` | `3.3` | Re-audited Saturday, August 15, 2026. Run detail provides the full investigation stack the bundle expects: DAG graph, payload capture when policy allows it, provider-call breakdown, tool/span inspection, and the backend-backed cancel action. It remains marked partial at the bundle level because supporting examples are still indirect and the cross-feature investigation bridges in the Observe matrix have not all been re-audited yet. External-rubric N/A review note: a run-detail surface should rarely be structurally unrelated to budgets, identity scope, runtime controls, or governance evidence, so current `N/A` usage looks too aggressive. |
| Sessions list | `/sessions` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | Keep separate. | P3 | `RE-AUDIT REQUIRED` | `3.4` | Re-audited Saturday, August 15, 2026. Sessions list is complete as an investigative read surface: grouped conversation rows, backend search/filter/pagination, CSV export, and clean pivots into session detail and runs are all present. The remaining partial is bundle-level cohesion rather than a missing sessions capability. External-rubric N/A review note: session-level observability usually has meaningful relationships to identity, spend, and investigation context, so several current `N/A` cells should be re-checked. |
| Session detail | `/sessions/{session_id}` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | Keep separate. | P3 | `RE-AUDIT REQUIRED` | `3.5` | Re-audited Saturday, August 15, 2026. Session detail does what the Observe workflow needs: ordered turn list, cumulative cost-over-turns chart, total duration/cost context, and per-turn pivots into full run detail. Like the rest of Bundle B, the remaining weakness is unresolved bundle cohesion rather than the session detail implementation itself. External-rubric N/A review note: session detail is typically part of the same investigative fabric as cost, scope, and runtime-policy evidence, so current `N/A` cells need a stricter review. |
| Request flow | `/request-flow` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `OK` | `PARTIAL` | Group with `Request flow focus` and `Request explorer` under one request analysis suite. | P3 | `RE-AUDIT REQUIRED` | `3.6` | Re-audited Saturday, August 15, 2026. Request Flow is the real causality view over `/runs/flow`: scope-aware Sankey modes, metric pivots, click-through into Request Explorer, and explicit bridges back to Analytics and Focus Mode are all working. It stays partial in the audit because example coverage is still light and the wider request-analysis cohesion story is not yet fully closed in the Observe matrix. External-rubric N/A review note: a request-causality surface should almost never be structurally unrelated to budget effects, identity scope, runtime controls, or guardrail evidence, so multiple current `N/A` cells should be revisited. |
| Request flow focus | `/request-flow/focus` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `PARTIAL` | `OK` | `PARTIAL` | Collapse into `Request flow` as a mode/state, not a distinct feature line. | P3 | `RE-AUDITED: PARTIAL` | `3.6` | Re-audited Saturday, August 15, 2026 as a deliberate mode-level collapse. The route is real and useful for large-canvas demos and dense debugging over the same `/runs/flow` contract, but it remains a sub-mode of Request Flow rather than an independently taught product surface. |
| Request explorer | `/request-explorer` | `OK` | `OK` | `PARTIAL` | `OK` | `OK` | `PARTIAL` | `PARTIAL` | `PARTIAL` | Group with `Request flow`; likely one request analysis area with tabs. | P3 | `RE-AUDIT REQUIRED` | `3.7` | Re-audited Saturday, August 15, 2026. The page and backend contract are real: `/analytics/request-explorer` powers filtering/pagination, and the UI exposes run, route, gateway, outcome, tool, and payload investigation with pivots into Runs and Sessions. The main remaining issue is support-surface accuracy: the docs still describe an outdated response shape (`total_count`/`records`) instead of the live `items`/`total` contract, and example/manual-lab coverage is thinner than the implemented page. External-rubric N/A review note: a request-exploration surface is normally expected to intersect with spend, identity, governance, and optimization loops, so current `N/A` cells should be re-audited. |
| Model usage | `/model-usage` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `PARTIAL` | `OK` | `PARTIAL` | Group with `Analytics economics` and `Cost and savings`. | P3 | `RE-AUDIT REQUIRED` | `3.12` | Re-audited Saturday, August 15, 2026. `/model-usage` is a real deep economics drilldown: scope-aware traffic mix, route/provider distribution, cost-latency-quality comparisons, and best-value signals are live in the UI and backed by the analytics APIs it consumes. The remaining partial is support-surface depth rather than missing product behavior: docs are aligned, but Postman and examples remain more adjacent to the underlying analytics family than narrowly centered on this exact page. External-rubric N/A review note: model-intelligence surfaces are normally tied to budgets, routing, cache posture, and governance signals, so several current `N/A` cohesion cells should be revisited. |
| Analytics economics | `/analytics/economics` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | Keep as the economics overview bridge; route users into `Model usage`, `Cost and savings`, and `Billing` instead of treating this as a competing deep drilldown. | P3 | `RE-AUDIT REQUIRED` | `3.9` | Re-audited Saturday, August 15, 2026. The page is correctly positioned as an overview bridge rather than a rival drilldown: top-cost workflows, version-compare impact, regressions, and annotations live here, and the UI explicitly hands users into Model Usage, Cost & Savings, and Billing Summary for deeper work. The only reason it stays partial is broader bundle cohesion, because the economics family still spans multiple owners and support surfaces instead of one tightly packaged subsystem. External-rubric N/A review note: an economics surface should usually have meaningful ties to runtime controls, governance, and optimization, so a number of current `N/A` cells likely need downgrade to `PARTIAL` or `GAP`. |
| Cost and savings | `/cost-savings` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `PARTIAL` | `OK` | `PARTIAL` | Group with `Analytics economics`, `Model usage`, and `Billing summary`. | P3 | `RE-AUDIT REQUIRED` | `3.18`, `6.3` | Re-audited Saturday, August 15, 2026. `/cost-savings` remains the primary optimization economics drilldown: spend-versus-saved framing, savings attribution, dimension pivots, ROI targeting, and budget rollup context are all live and documented. It stays partial in the matrix because support coverage is shared across the broader FinOps analytics stack, with less feature-specific Postman and runnable example material than the strength of the page itself would suggest. External-rubric N/A review note: avoided-cost and savings views are structurally connected to cache, rate limits, budgets, and optimization loops, so the current `N/A` use across some intersections should be revisited. |
| Billing summary | `/billing-summary` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `OK` | `OK` | Collapsed into `Billing` as a summary tab/view; keep this route only as a compatibility redirect. | P3 | `RE-AUDITED: OK` | `6.9` | Re-audited Saturday, August 15, 2026 as a deliberate collapse. `/billing-summary` is intentionally no longer an independent owner: the route redirects to `/billing?tab=summary&months=6`, the live summary UI and exports live inside Billing, and the docs describe that collapse explicitly. Supporting scripts still hit the compatibility API surface under `/budgets/billing-summary`, but that is consistent with the product direction rather than a split-brain UI problem. |
| Outcomes and ROI | `/outcomes` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | Keep separate. | P3 | `RE-AUDITED: OK` | `3.19` | Re-audited Saturday, August 15, 2026. Outcomes is fully closed as both operator ledger and ROI analysis surface: CRUD plus summary, trend, workflow ROI, and quality-correlation endpoints all exist, the UI exposes create/filter/edit/delete alongside the analytics views, and docs/Postman/examples/labs all match the shipped lifecycle. This is one of the stronger Bundle C features because the workflow from recording business value to inspecting cost-per-success is explicit and complete. |
| Analytics users | `/analytics/users` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | Keep separate. | P3 | `RE-AUDIT REQUIRED` | `3.10` | Re-audited Saturday, August 15, 2026. The list page is complete for the investigative use case it serves: top spenders, cohort tiers, anomaly flags, and quick segmentation tabs are real and backed by dedicated analytics endpoints. Docs, Postman, and examples now teach the page directly, and the workflow bridges into Request Explorer, Request Flow, and Outcomes & ROI make the surface cohesive instead of isolated. External-rubric N/A review note: user-analytics surfaces are usually expected to connect to identity, spend, runtime, and governance posture, so current `N/A` cells should be reconsidered. |
| Analytics user detail | `/analytics/users/{end_user_id}` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | Keep separate. | P3 | `RE-AUDITED: OK` | `3.10` | Re-audited Saturday, August 15, 2026. The per-user detail page is a complete investigative drilldown: spend over time, model mix, feature usage, and direct pivots into Request Explorer, Request Flow, and Outcomes are all live against the dedicated backend route. This feature does not need CRUD to be complete because its job is evidence and navigation, and the support surfaces now reflect that clearly. |
| Model scorecards | `/model-scorecards` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `OK` | `PARTIAL` | Keep separate. | P3 | `RE-AUDITED: PARTIAL` | `3.14` | Re-audited Saturday, August 15, 2026. The page has a clear and useful role in the workflow: after Model Usage or Request Flow identifies candidate lanes, Model Scorecards compares models on cost, latency, reliability, cache behavior, quality, and trends, then points users into Evaluation Studio or Replay Lab before routing changes. It remains partial only because the support story is still lighter than the implementation, with less dedicated example and script material than adjacent analytics and evaluation surfaces. |
| Engineering | `/engineering` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `OK` | `PARTIAL` | Keep separate. | P3 | `RE-AUDITED: PARTIAL` | `3.11` | Re-audited Saturday, August 15, 2026. Engineering is a real operator read surface rather than a missed CRUD feature: `GET /analytics/engineering` ships the latency, retry, cache, lifecycle, cost-breakdown, and quality-funnel payload the page renders, and the UI pivots cleanly into Request Explorer, Request Flow, and Model Usage. It remains partial only because supporting examples and scenario coverage are still thinner than the implementation and docs/Postman surface. |
| Monitoring | `/monitoring` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `PARTIAL` | `OK` | `PARTIAL` | Keep as the operations triage shell; deep setup and configuration stay in Security, Alert Rules, Gateway, and Telemetry. | P3 | `RE-AUDIT REQUIRED` | `3.13` | Re-audited Saturday, August 15, 2026. Monitoring works as the intended triage shell: the page aggregates security events, alert firings, and gateway-request evidence, then hands operators into Telemetry, Security, and other deeper owners instead of duplicating them. It stays partial because the support story is distributed across underlying surfaces and legacy OTLP naming still appears in docs and labs even though `/otlp` now redirects correctly to `/monitoring/telemetry`. External-rubric N/A review note: an operations monitoring shell should usually relate to budgets, identity scope, runtime governance, and evidence chains, so several current `N/A` cells should be re-audited. |
| Telemetry | `/monitoring/telemetry` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | Keep separate as the deep OTLP ingest owner; preserve `/otlp` as a compatibility redirect only. | P3 | `RE-AUDIT REQUIRED` | `3.13` | Re-audited Saturday, August 15, 2026. Telemetry is a real deep observability surface: OTLP stats, ingest batches, attribution coverage, semantic dimensions, and batch-detail inspection are all live under `/monitoring/telemetry`, while `/otlp` redirects there for compatibility. It remains partial only at the family level because route naming is still split across support surfaces and the page is intentionally a specialized owner rather than the main monitoring shell. External-rubric N/A review note: telemetry is foundational to runtime evidence, cost attribution, governance signals, and optimization loops, so current `N/A` cells should be reviewed under a stricter standard. |
| Quality scores | `/evaluations` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | Merged with `Evaluation studio`, `Datasets`, `Experiments`, and `Replay lab` into one evaluation suite. | P3 | `RE-AUDITED: OK` | `3.20`, `7.2`, `7.3` | Re-audited Saturday, August 15, 2026 as a deliberate collapse. `/evaluations` is intentionally no longer a standalone Observe owner: the route redirects to `/evaluation?tab=scores`, while score submission, listing, evaluator flows, and experiments live inside the evaluation suite with matching docs, Postman coverage, tests, and examples. This collapse is healthy because it preserves quality-score visibility without recreating a parallel observability page. |
| Replay lab | `/replay` | `OK` | `OK` | `OK` | `PARTIAL` | `OK` | `OK` | `PARTIAL` | `PARTIAL` | Collapse route ownership into `/replay`; keep `Replay Lab` as the user-facing feature name rather than introducing a separate `/replay-lab` route. | P3 | `RE-AUDITED: PARTIAL` | `3.15` | Re-audited Saturday, August 15, 2026. Replay Lab is a real surface backed by `/replay/datasets`, `/replay/experiments`, `/replay/experiments/{id}/run`, and `/replay/experiments/{id}/results`, with the dashboard supporting dataset creation, experiment management, result comparison, and route recommendations from experiment detail. It stays partial because the shipped route is `/replay` with no compatibility `/replay-lab` route, while docs and README still teach the feature primarily by its label rather than its canonical dashboard path. |
| Runbooks | `/runbooks` | `PARTIAL` | `OK` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `OK` | `PARTIAL` | `PARTIAL` | Keep separate. | P3 | `RE-AUDITED: PARTIAL` | `3.16` | Re-audited Saturday, August 15, 2026. Runbooks are meaningfully implemented: the page lists generated entries, supports severity filters, on-demand generation from a run ID, and run-linked investigation, while the backend exposes create and list endpoints and the runbook-generation service builds real summaries from runs, spans, provider calls, and tool calls. The main gap is export completeness: the UI calls `/runs/runbooks/{id}/export`, but no matching backend export route is present, and the docs currently describe markdown/JSON export as if it is fully shipped. |
| Optimization simulator | `/optimization-simulator` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | `OK` | `PARTIAL` | Keep separate. | P3 | `RE-AUDITED: PARTIAL` | `3.17` | Re-audited Saturday, August 15, 2026. The Optimization Simulator is a real what-if surface over `POST /analytics/simulate-optimization`: the UI supports model swap, intent window, cache/compression toggles, and projected savings/latency/risk output, and the backend computes projections from live provider-call telemetry plus a built-in model catalog. It remains partial because support coverage is thinner than the page itself: there is Postman and docs coverage, but no dedicated example, lab, or API test exercising the simulator contract directly. |
| Organization dashboard | `/organization/dashboard` | `OK` | `OK` | `OK` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `OK` | `OK` | Merged into `Analytics overview` as an org-scoped compatibility redirect. | P3 | `RE-AUDITED: OK` | `1.2`, `3.1` | Re-audited Saturday, August 15, 2026. `/organization/dashboard` is a deliberate compatibility redirect into `/analytics?scope=org&view=overview`, while the actual org-level data path remains backed by `GET /org/dashboard` and the scoped Analytics shell. This redirect feature is closed; the only softness is that some README/demo wording still talks about dashboard homes generically rather than the shared `/analytics` owner. |
| Global dashboard | `/global-dashboard` | `OK` | `OK` | `OK` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `OK` | `OK` | Merged into `Analytics overview` as a platform-scoped compatibility redirect. | P3 | `RE-AUDITED: OK` | `1.7`, `3.1` | Re-audited Saturday, August 15, 2026. `/global-dashboard` now exists only as a compatibility redirect into `/analytics?scope=platform&view=overview`, preserving old entry points while keeping platform-level overview ownership in the shared Analytics shell. As with the other collapsed dashboard routes, the remaining weakness is support-surface naming consistency rather than missing runtime behavior. |
