# Planned Architecture — GAP Matrix

Last updated: 2026-08-15

## Legend

| Status | Meaning |
|--------|---------|
| `PENDING` | Not yet audited |
| `OK` | Verified working |
| `PARTIAL` | Present but partial, buggy, or unclear |
| `MISSING` | Missing, broken, or disconnected |
| `LEGACY` | Legacy/transitional surface; do not expand |

---

## Feature Rows

| Feature | Route | Backend | UI | Actions | Docs | Postman | Scripts/Examples | Complete | Cohesion | Merge / Collapse | Fix Order | Fix Status | Delivery Audit Crosswalk | Notes |
|---------|-------|---------|----|---------|------|---------|------------------|----------|----------|------------------|-----------|------------|--------------------|-------|
| High-performance gateway service split | `planned service: runledger-gateway-rs` | `PARTIAL` | `MISSING` | `PARTIAL` | `OK` | `OK` | `OK` | `PARTIAL` | `PARTIAL` | Split the inline gateway/runtime path out of the current API service; keep control-plane config in `/gateway` but move hot-path execution into a separately scalable Rust service. | P0 | `RE-AUDITED: PARTIAL` | `4.1`, `4.3`, `4.4`, `4.5`, `4.8`, `4.10` | The hard cut is materially in place now: `apps/gateway-rs` exists as the live data plane, the Python `/gateway/chat/completions` path is a 410 compatibility stub, and the control plane exposes runtime snapshot, preflight, finalize, and signed-event contract endpoints. Docs, Postman, scripts, and examples already cover the split well. This remains PARTIAL overall because the broader architecture convergence is not finished yet and there is no dedicated in-product UI surface for the split itself. |
| Review refactored gateway modules and migrate remaining hot data path into Rust | `cross-cutting: gateway router split` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `OK` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | Keep the refactored Python router split as the control-plane layout, but move any remaining live request-path logic out of Python and into `runledger-gateway-rs`. | P0 | `RE-AUDIT REQUIRED` | `4.1`, `4.3`, `4.4`, `4.5`, `4.8`, `4.10`, `apps/api/runledger_api/routers/gateway*.py` | The refactored gateway module layout is real and should be kept: `gateway.py` is a thin aggregator and the router family now separates legacy, routing, passthrough, runtime, and observability concerns much more clearly. But the review outcome is still PARTIAL because `gateway_runtime.py` plus helpers pulled through `gateway_shared.py` continue to perform substantial preflight hot-path work in Python, including auth resolution, cache lookup, routing prep, guardrails, and fallback planning. The boundary is clearer, but the migration of latency-sensitive execution support into Rust is not finished. External-rubric N/A review note: even a backend-architecture cleanup usually has operator-UI and product-surface implications around health, visibility, and deprecated-path removal, so `N/A` should be re-checked here. |
| Collapse `runledger-router` into the Rust gateway | `planned service merge: runledger-router -> runledger-gateway-rs` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `OK` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | Fold the hot-path intelligent routing classifier into the Rust gateway runtime; keep Python control-plane routing/admin APIs separate and do not confuse runtime decisioning with admin/config routing surfaces. | P0 | `RE-AUDIT REQUIRED` | `4.3`, `4.4`, `4.5`, `4.8`, `4.10`, `docs/gateway/*`, `docker-compose.yml` | The architecture target is well-defined and strongly supported by the current code review: `runledger-router` is still a narrow sidecar reached through `apps/api/runledger_api/services/intelligent_router.py`, and that classify call still sits directly on the Python preflight hot path. `docker-compose.yml` still provisions `runledger-router` separately, and docs/examples still describe it as a distinct service. This remains PARTIAL because the collapse has not happened yet, even though the end-state recommendation is now much clearer and the Rust gateway is the obvious destination. External-rubric N/A review note: this kind of service-boundary change usually has UI, docs, and operator-workflow implications, so blanket `N/A` treatment is too strong. |
| Legacy Python gateway deprecation and consumer migration | `cross-cutting: docs/examples/postman/scripts` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | Collapse all remaining inline-gateway assumptions into the Rust runtime model and remove stale Python-hosted completion references. | P0 | `RE-AUDITED: PARTIAL` | `4.1`, `4.3`, `4.8`, `reference/api`, `docs/*`, `scripts/*`, `examples/*` | The deprecation is real: the Python `/gateway/chat/completions` route is now a 410 compatibility stub and the docs/spec explicitly state that `runledger-gateway-rs` is the only live runtime as of Friday, August 14, 2026. But the migration cleanup is still incomplete because Postman, examples, benchmarks, scenario labs, and generator text continue to carry broad `/gateway/chat/completions` references and mixed control-plane/data-plane guidance. The direction is correct, but the repo has not fully shed its legacy inline-runtime assumptions yet. |
| Pipeline studio and flow builder | `/pipeline-studio` | `MISSING` | `MISSING` | `MISSING` | `PARTIAL` | `MISSING` | `MISSING` | `MISSING` | `PARTIAL` | New surface; this should unify visualization and authoring instead of spawning separate "diagram" and "builder" products. | P0 | `RE-AUDITED: MISSING` | `2.3`, `2.5`, `2.6`, `3.6`, `4.1`, `6.4` | This remains a future concept rather than a shipped feature. The only real evidence today is documentation-level architecture intent in files like `docs/architecture.md`, `docs/platform-overview.mdx`, and `docs/gateway/gateway-rs-spec.mdx`. There is no app route, backend API, Postman coverage, or supporting script/example surface yet. Cohesion is only partial at the concept level because the planned workflow is well aligned to routing, governance, observability, and build loops, but none of that exists as a product surface today. |
| API explorer and generated Swagger UI | `/api-docs` | `OK` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | Collapse manual Postman-first maintenance into generated OpenAPI/Swagger plus an embedded GUI explorer. | P1 | `RE-AUDITED: PARTIAL` | `reference/api` | OpenAPI is already live and the API now exposes a real browser reference surface via Scalar at `/reference`, so this is no longer just aspirational. But it remains PARTIAL because the planned destination is still not fully converged: the route naming in the plan does not match the shipped surface, docs still describe the explorer as a planned direction, Postman remains a major parallel maintenance path, and the explorer is not yet presented as a more embedded product-native control-plane experience. |
| In-app customer documentation and help hub | `planned top-right help / docs hub` | `MISSING` | `MISSING` | `MISSING` | `PARTIAL` | N/A | `MISSING` | `MISSING` | `PARTIAL` | New surface; do not scatter help links randomly across pages. | P1 | `RE-AUDITED: MISSING` | `1.8`, `docs/*` | This is still only a planned surface. The repo has extensive external docs, but there is no actual top-right help hub, embedded customer guidance panel, or product-native contextual help flow in the app today. Docs make the concept plausible, so docs/cohesion are only partial at the intent level, but the feature itself remains missing. |
| UI theme refresh and dark-mode redesign | `cross-app` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | N/A | `PARTIAL` | `PARTIAL` | `PARTIAL` | One cross-app design system pass, not page-by-page ad hoc recoloring. | P1 | `RE-AUDITED: PARTIAL` | `apps/web/app/globals.css`, `apps/web/components/*` | The app has a real shared theme system, semantic CSS variables, theme toggling, and a cross-app styling baseline, so this is not missing. But `apps/web/app/globals.css` still includes broad legacy compatibility overrides and explicit notes about softening old dark-mode utilities, which is strong evidence that the redesign is still transitional rather than a completed design-system pass. The current state is usable, but not yet a clean, fully intentional visual refresh. |
| Documentation IA, hierarchy, and diagrams | `docs/` | `PARTIAL` | N/A | N/A | `PARTIAL` | N/A | N/A | `PARTIAL` | `PARTIAL` | Reorganize docs by product workflow and architecture domains instead of historical/random accretion. | P1 | `RE-AUDITED: PARTIAL` | `docs/introduction.mdx`, `docs/architecture.md`, `docs/mint.json` | The docs IA has clearly moved beyond raw historical accretion: `docs/mint.json` already groups content by product domains, `docs/introduction.mdx` explains the platform through a workflow-centered journey, and `docs/architecture.md` includes meaningful topology and future-state diagrams. This remains PARTIAL because the information architecture is still in transition, some planned surfaces are described as future concepts, and the repo still mixes polished product hierarchy with older reference- and implementation-shaped organization. |
| Broader repo naming and historical cleanup review | `cross-cutting: tests/scripts/docs` | `PARTIAL` | N/A | `PARTIAL` | `PARTIAL` | N/A | `PARTIAL` | `PARTIAL` | `PARTIAL` | Keep as a final consistency pass; remove historical phase-style naming and delivery-era labels where they no longer help operators or developers. | P1 | `RE-AUDITED: PARTIAL` | `docs/*`, `scripts/*`, `apps/api/tests/*` | This cleanup is still meaningfully unfinished. The repo now has stronger product-centered language in many top-level docs, but there are still many visible `Phase X`, `Bundle X`, and rollout-era labels across tests, scenario scripts, smoke scripts, upgrade guidance, and supporting docs. The need is clear and lower-risk than runtime work, but the naming layer has not yet been broadly normalized. |
| Scope-aware runtime governance enforcement deepening | `cross-cutting: gateway + tool governance hot path` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | `PARTIAL` | Keep Tool Registry and Tool Policies as the control-plane UX, but deepen enforcement in the runtime path so scope context is enforced directly on live traffic. | P1 | `RE-AUDIT REQUIRED` | `4.1`, `4.5`, `7.13`, `7.14`, `apps/api/runledger_api/services/plugin_runner.py`, `apps/api/runledger_api/routers/workspace_controls.py`, `runledger-gateway-rs` | Governance is materially stronger now that Tool Policies, Access Groups, and related workspace controls are real control-plane primitives, and the runtime already enforces some live checks through gateway preflight, metadata/IP validation, guardrails, and tool-call governance. But this is still PARTIAL because policy matching remains centered mostly on tool/action and coarse request context. Richer scope identity such as access-group ownership, broader actor scope, and clearer propagation of scope evidence into live runtime decisions and logs has not been fully pushed through the hot path yet. External-rubric N/A review note: deeper runtime enforcement almost always implies UI visibility, evidence surfaces, and operator workflows, so `N/A` is likely too strong here. |
