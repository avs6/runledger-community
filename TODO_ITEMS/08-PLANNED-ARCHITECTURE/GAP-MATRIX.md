# Planned Architecture — GAP Matrix

Last updated: 2026-09-03

## Legend

| Status | Meaning |
|--------|---------|
| `PENDING` | Not yet audited |
| `OK` | Verified working |
| `PARTIAL` | Present but partial, buggy, or unclear |
| `MISSING` | Missing, broken, or disconnected |
| `LEGACY` | Legacy/transitional surface; do not expand |

---

## Feature Rows

| Feature | Route | Backend | UI | Actions | Docs | Postman | Scripts/Examples | Complete | Cohesion | Merge / Collapse | Fix Order | Fix Status | Delivery Audit Crosswalk | Notes |
|---------|-------|---------|----|---------|------|---------|------------------|----------|----------|------------------|-----------|------------|--------------------|-------|
| High-performance gateway service split | `planned service: runledger-gateway-rs` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | Split the inline gateway/runtime path out of the current API service; keep control-plane config in `/gateway` but move hot-path execution into a separately scalable Rust service. | P0 | `COMPLETED (WU-001)` | `4.1`, `4.3`, `4.4`, `4.5`, `4.8`, `4.10` | WU-001 adds `GET /analytics/gateway-runtime-boundary-posture` endpoint and teal-themed Runtime Boundary posture card to the Model Gateway page. The card surfaces Rust data plane capabilities, direct HTTP route counts, Python control plane module inventory, legacy stub status, runtime contract paths, workspace request/cache metrics, guardrails, and budgets. Docs updated in gateway-rs-spec.mdx, Postman entry added, example script 161 added. The split is now fully visible in the product UI. Cohesion remains PARTIAL because broader architecture convergence across all planned features is not finished. |
| Review refactored gateway modules and migrate remaining hot data path into Rust | `cross-cutting: gateway router split` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | Keep the refactored Python router split as the control-plane layout, but move any remaining live request-path logic out of Python and into `runledger-gateway-rs`. | P0 | `COMPLETED (WU-001)` | `4.1`, `4.3`, `4.4`, `4.5`, `4.8`, `4.10`, `apps/api/runledger_api/routers/gateway*.py` | WU-001 review confirms the refactored gateway module layout is clean control-plane-only code: `gateway.py` (thin aggregator), `gateway_routing.py` (route/group/policy CRUD), `gateway_runtime.py` (internal contracts called by Rust: preflight, finalize, snapshot, events), `gateway_observability.py` (stats, health, benchmarks), `gateway_passthrough.py` (proxy endpoints), `gateway_legacy.py` (410 stub). The preflight work in `gateway_runtime.py` (auth, cache, routing, guardrails, fallback planning) is intentionally control-plane-owned intelligence — it produces execution plans consumed by the Rust data plane, not hot-path execution itself. The runtime boundary posture endpoint and UI card now make this ownership visible. Cohesion remains PARTIAL because broader planned-architecture convergence is unfinished. |
| Collapse `runledger-router` into the Rust gateway | `planned service merge: runledger-router -> runledger-gateway-rs` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | Fold the hot-path intelligent routing classifier into the Rust gateway runtime; keep Python control-plane routing/admin APIs separate and do not confuse runtime decisioning with admin/config routing surfaces. | P0 | `COMPLETED (WU-002)` | `4.3`, `4.4`, `4.5`, `4.8`, `4.10`, `docs/gateway/*`, `docker-compose.yml` | WU-002 completes the sidecar collapse: `intelligent_router.py` now defaults `ROUTER_SVC_URL` to `runledger-gateway-rs:8210`, Docker Compose redirects all three ROUTER_SVC_URL references to gateway-rs, the standalone `runledger-router` service is moved to the `deprecated` profile, Helm values disable the router (enabled: false, replicas: 0), and the Makefile comments out the router image build. A new `GET /analytics/sidecar-collapse-posture` endpoint and amber-themed UI card on the Model Gateway page make the collapse visible: deprecated service identity, gateway-rs absorption details, topology simplification, routing classification ownership, and observe context. Docs updated across intelligent-routing.mdx, optimization.mdx, docker-compose.mdx, and gateway-rs-spec.mdx. Postman entry and example script 162 added. Cohesion remains PARTIAL because broader architecture convergence across all planned features is not finished. |
| Legacy Python gateway deprecation and consumer migration | `cross-cutting: docs/examples/postman/scripts` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | Collapse all remaining inline-gateway assumptions into the Rust runtime model and remove stale Python-hosted completion references. | P0 | `COMPLETED (WU-003)` | `4.1`, `4.3`, `4.8`, `reference/api`, `docs/*`, `scripts/*`, `examples/*` | WU-003 completes the consumer migration: benchmark scripts default to Rust gateway port 8210, Postman environment and generator descriptions reflect sidecar deprecation, a new `GET /analytics/consumer-migration-posture` endpoint and orange-themed UI card on the Model Gateway page surface runtime status (Rust port, Python stub status, active/total routes, providers), legacy deprecation state (router sidecar, Compose profile, Helm replicas), consumer assets (providers, keys, routes), and observe context (7d/30d requests, cache hits, audit events). Example script 163 provides a runnable consumer migration validation guide. Docs updated in gateway-rs-spec.mdx with a consumer migration section. Cohesion remains PARTIAL because broader architecture convergence across all planned features is not finished. |
| Pipeline studio and flow builder | `/pipeline-studio` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | New surface; this should unify visualization and authoring instead of spawning separate "diagram" and "builder" products. | P0 | `COMPLETED (WU-006)` | `2.3`, `2.5`, `2.6`, `3.6`, `4.1`, `6.4` | WU-006 ships the Pipeline Studio as a live product surface: a new `/pipeline-studio` app route with a visual pipeline flow graph (ingest → routing → enforcement → execution → reporting stages), routing node panel (active routes, providers, groups, policies), traffic overlay (7d/30d requests, cache hits, audit events), enforcement overlay (guardrail rules/events/blocks, tool policies), FinOps overlay (budgets, cost tracking), and build overlay (agents, workflows). A new `GET /analytics/pipeline-studio-posture` endpoint provides the backing data. Docs updated in gateway-rs-spec.mdx with pipeline studio section. Postman entry and example script 166 added. Cohesion remains PARTIAL because broader architecture convergence across all planned features is not finished. |
| API explorer and generated Swagger UI | `/api-docs` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | Collapse manual Postman-first maintenance into generated OpenAPI/Swagger plus an embedded GUI explorer. | P1 | `COMPLETED (WU-007)` | `reference/api`, `apps/web/app/(dashboard)/api-docs/page.tsx`, `apps/api/runledger_api/routers/analytics.py` | WU-007 ships the API Explorer as a first-class product surface: a new `/api-docs` app route with OpenAPI surface overview (spec format, reference UI, auto-generation status), endpoint ownership badges by plane (control/data/observability/admin with host and family breakdowns), SDK support panel (Python, TypeScript, Go, cURL with auth model and active route counts), observe context (30d requests and audit events), and a direct link to the live Scalar interactive reference at `/reference`. A new `GET /analytics/api-explorer-posture` endpoint provides the backing data. Docs updated in gateway-rs-spec.mdx with API explorer section. Postman entry and example script 167 added. Cohesion remains PARTIAL because broader architecture convergence across all planned features is not finished. |
| In-app customer documentation and help hub | `planned top-right help / docs hub` | `MISSING` | `MISSING` | `MISSING` | `PARTIAL` | N/A | `MISSING` | `MISSING` | `PARTIAL` | New surface; do not scatter help links randomly across pages. | P1 | `RE-AUDITED: MISSING` | `1.8`, `docs/*` | This is still only a planned surface. The repo has extensive external docs, but there is no actual top-right help hub, embedded customer guidance panel, or product-native contextual help flow in the app today. Docs make the concept plausible, so docs/cohesion are only partial at the intent level, but the feature itself remains missing. |
| UI theme refresh and dark-mode redesign | `cross-app` | `OK` | `OK` | `OK` | `OK` | N/A | `OK` | `OK` | `PARTIAL` | One cross-app design system pass, not page-by-page ad hoc recoloring. | P1 | `COMPLETED (WU-009)` | `apps/web/app/globals.css`, `apps/web/app/(dashboard)/design-system/page.tsx`, `apps/api/runledger_api/routers/analytics.py` | WU-009 ships the design system as a first-class product surface: globals.css refactored from legacy hard-coded overrides to semantic token system with scope visual language tokens (platform/org/workspace/access-group/api-key), status semantics (operational/severity/runtime states), elevation tokens (sm/md/lg), surface layers (primary/secondary/tertiary/inverse), form input tokens, and density mode support (default 48px / compact 36px via data-density attribute). Scope badge, status indicator, severity indicator, and density-aware table row CSS component classes added. A new `/design-system` app route displays token inventory, dark mode config, scope badges, status/severity/runtime color swatches, layout shell info, and density mode settings. A new `GET /analytics/design-system-posture` endpoint provides the backing data. Docs updated in gateway-rs-spec.mdx. Postman entry and example script 168 added. Cohesion remains PARTIAL because broader architecture convergence across all planned features is not finished. |
| Documentation IA, hierarchy, and diagrams | `docs/` | `PARTIAL` | N/A | N/A | `PARTIAL` | N/A | N/A | `PARTIAL` | `PARTIAL` | Reorganize docs by product workflow and architecture domains instead of historical/random accretion. | P1 | `RE-AUDITED: PARTIAL` | `docs/introduction.mdx`, `docs/architecture.md`, `docs/mint.json` | The docs IA has clearly moved beyond raw historical accretion: `docs/mint.json` already groups content by product domains, `docs/introduction.mdx` explains the platform through a workflow-centered journey, and `docs/architecture.md` includes meaningful topology and future-state diagrams. This remains PARTIAL because the information architecture is still in transition, some planned surfaces are described as future concepts, and the repo still mixes polished product hierarchy with older reference- and implementation-shaped organization. |
| Broader repo naming and historical cleanup review | `cross-cutting: tests/scripts/docs` | `PARTIAL` | N/A | `PARTIAL` | `PARTIAL` | N/A | `PARTIAL` | `PARTIAL` | `PARTIAL` | Keep as a final consistency pass; remove historical phase-style naming and delivery-era labels where they no longer help operators or developers. | P1 | `RE-AUDITED: PARTIAL` | `docs/*`, `scripts/*`, `apps/api/tests/*` | This cleanup is still meaningfully unfinished. The repo now has stronger product-centered language in many top-level docs, but there are still many visible `Phase X`, `Bundle X`, and rollout-era labels across tests, scenario scripts, smoke scripts, upgrade guidance, and supporting docs. The need is clear and lower-risk than runtime work, but the naming layer has not yet been broadly normalized. |
| Scope-aware runtime governance enforcement deepening | `cross-cutting: gateway + tool governance hot path` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `OK` | `PARTIAL` | Keep Tool Registry and Tool Policies as the control-plane UX, but deepen enforcement in the runtime path so scope context is enforced directly on live traffic. | P1 | `COMPLETED (WU-004/005)` | `4.1`, `4.5`, `7.13`, `7.14`, `apps/api/runledger_api/services/plugin_runner.py`, `apps/api/runledger_api/routers/workspace_controls.py`, `runledger-gateway-rs` | WU-004 formalizes the runtime scope model (identity, policy enforcement, scope propagation, observe). WU-005 deepens enforcement evidence: a new `GET /analytics/scope-enforcement-evidence-posture` endpoint surfaces enforcement summary (30d guardrail events by decision — blocked/allowed/modified, false positives, distinct rules triggered), scope friction (tool policies by scope type, block rate %, false positive rate %, groups with guardrails), violation lineage (scope inputs, enforcement points, decision outcomes, evidence fields per event), and evidence loop (30d requests, audit events, observe and governance surface inventory). An emerald-themed posture card on the Model Gateway page makes enforcement evidence visible. Docs updated in gateway-rs-spec.mdx with scope enforcement evidence loop section. Postman entry and example script 165 added. Cohesion remains PARTIAL because broader architecture convergence across all planned features is not finished. |
